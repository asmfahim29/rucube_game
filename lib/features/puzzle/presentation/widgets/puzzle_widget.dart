import 'package:flutter/material.dart';

class PuzzleWidget extends StatelessWidget {
  const PuzzleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implement your widget
      child: const Text('Puzzle Widget'),
    );
  }
}
